# Copy to `hosts` (gitignored) and adjust for your network.
# ansible-playbook run.yml -K

[server]
your-nas ansible_host=192.168.1.10 ansible_user=deploy ansible_connection=ssh ansible_ssh_private_key_file=~/.ssh/id_ed25519
